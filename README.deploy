# How to Deploy Flow Logix Library 

# To deploy a release:
mvn -Prelease-flowlogix release:prepare -DdryRun=true
mvn -Prelease-flowlogix release:perform -DdryRun=true

# To deploy a snapshot
mvn -Prelease-flowlogix clean source:jar javadoc:jar deploy
